import uglify from 'rollup-plugin-uglify';
import { minify } from 'uglify-js-harmony';

export default {
  entry: 'lib/browser.js',
  format: 'umd',
  dest: 'js-logging.browser.min.js',
  moduleName: 'Logging',
  plugins: [
    uglify({}, minify)
  ]
};